// GENERATED by `python tools/course/turf.py --js`. Do not hand-edit:
// tests/ground-tiles-match-their-maps.test.js reads the PNGs and fails on a diff.
//
// The ground surfaces the player looks at for most of every outdoor frame.
// Each is a periodic height field with a normal, an occlusion and a roughness
// derived from it and from nothing else (tools/course/turf.py), sized against
// the photographs in Designs/Course/Ground.

export const GROUND_TILE_M = 1.0;
export const GROUND_TILE_PX = 512;

export const GROUND_TILES = {
  turf_close: {
    albedo: 'ground/turf_close_alb.png',   // RGB colour (sRGB), A roughness
    normal: 'ground/turf_close_nrm.png',   // RG normal xy, B occlusion, A height
    reliefMm: 2.2,
    roughness: 0.86,
    meanLinear: [0.06684, 0.18060, 0.04110],
    note: "fairway / semi / tee / green / fringe -- the lay runs along +u",
    reference: "Designs/Course/Ground/fairway/hole_and_path.jpg, turf_close/sod_farm.jpg",
  },
  turf_rough: {
    albedo: 'ground/turf_rough_alb.png',   // RGB colour (sRGB), A roughness
    normal: 'ground/turf_rough_nrm.png',   // RG normal xy, B occlusion, A height
    reliefMm: 7.5,
    roughness: 0.93,
    meanLinear: [0.04132, 0.10103, 0.02029],
    note: "rough / heavy / scrub -- clumped, four times the relief, near-isotropic",
    reference: "Designs/Course/Ground/rough/halle_westf.jpg, fairway/hole_and_path.jpg (left of the path)",
  },
  sand: {
    albedo: 'ground/sand_alb.png',   // RGB colour (sRGB), A roughness
    normal: 'ground/sand_nrm.png',   // RG normal xy, B occlusion, A height
    reliefMm: 1.4,
    roughness: 0.74,
    meanLinear: [0.70559, 0.65853, 0.55068],
    note: "bunker sand -- near white and barely saturated, NOT the orange tan the shader used",
    reference: "Designs/Course/Ground/bunker/atalaya.jpg",
  },
  hard: {
    albedo: 'ground/hard_alb.png',   // RGB colour (sRGB), A roughness
    normal: 'ground/hard_nrm.png',   // RG normal xy, B occlusion, A height
    reliefMm: 1.7,
    roughness: 0.85,
    meanLinear: [0.35950, 0.34811, 0.31680],
    note: "cart path concrete -- a light neutral grey, not the warm dirt the path zone tinted",
    reference: "Designs/Course/Ground/fairway/hole_and_path.jpg, path/metairie.jpg",
  },
};

// zone -> [surface, multiplier]. multiplier = target mean / tile mean, in
// linear light, so the tile's AVERAGE lands on the colour that was tuned in
// the game and its variation is what is new. See ZONES in turf.py.
export const GROUND_ZONES = {
  fair: { surface: 'turf_close', mul: [1.7399, 1.3948, 1.5084] },  // tuned parkland fairway, unchanged
  semi: { surface: 'turf_close', mul: [1.6815, 1.1799, 1.3965] },  // first cut, unchanged
  green: { surface: 'turf_close', mul: [2.0869, 1.5448, 1.5473] },  // unchanged
  fringe: { surface: 'turf_close', mul: [1.7399, 1.2873, 1.3211] },  // unchanged
  tee: { surface: 'turf_close', mul: [1.9478, 1.4590, 1.5838] },  // unchanged
  rough: { surface: 'turf_rough', mul: [1.8588, 1.2313, 2.1385] },  // unchanged
  heavy: { surface: 'turf_rough', mul: [2.1783, 1.1789, 2.2173] },  // unchanged
  scrub: { surface: 'turf_rough', mul: [2.4421, 1.3214, 2.9219] },  // native, unchanged
  bed: { surface: 'turf_rough', mul: [3.3038, 0.8809, 2.6312] },  // dark mulch, unchanged
  waterbed: { surface: 'turf_rough', mul: [1.8661, 1.2036, 2.6312] },  // unchanged
  sand: { surface: 'sand', mul: [1.0000, 1.0000, 1.0000] },  // DEPARTS: the tile's own near-white, off bunker/atalaya.jpg
  dirt: { surface: 'hard', mul: [0.7191, 0.5481, 0.3886] },  // worn earth, unchanged
  path: { surface: 'hard', mul: [1.0000, 1.0000, 1.0000] },  // DEPARTS: the tile's own light concrete, off fairway/hole_and_path.jpg
};
