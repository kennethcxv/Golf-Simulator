# Conflicts resolved

Most serious conflicts were semantic: Git produced a clean merge while one branch's caller no longer matched another branch's runtime authority.

| Area | Resolution |
|---|---|
| Main/UI loop | Restored missing UI, maintenance, golf-day, delivery, confirmation, and input-handler dependencies; prevented the `I` input case from falling through to vehicle lights. |
| Course scene | Rejoined editor, maintenance, shader, tool-socket, render-budget, focus, and interaction helpers around one scene lifecycle. |
| Municipal clubhouse | Scoped the failing-municipal presentation to Willow Creek and preserved the live register, delivery cartons, carry interaction, counter hardware, and cleaning/restoration props. |
| Checkout generations | Retained one physical scanner/card/cash/drawer/receipt/bag flow, restored removed helper definitions, removed obsolete competing gesture paths, and fixed customer objects leaking as `[object Object]` into ledger descriptions. |
| Shop/layout | Reconciled authored furniture, generated service rooms, stable fixture IDs, occupancy, stored ownership, and stock conservation through the existing layout authority. |
| Delivery | Aligned package model stems, dimensions, rotation, receiving state, box placement, and product lookup without overwriting raw source assets. |
| Save migrations | Ordered domain restoration through state schema 16 and empire schema 3; canonical lots own migrated inventory while legacy totals remain compatibility inputs only. |
| Generated reservations | Allocated stable customer identities at generation time so serialization is a pure snapshot and does not grow the customer directory. |
| Physical golfer arrivals | Reconnected the orphaned clubhouse arrival update, paced simultaneous presentation spawns, made automatic doors passable to the final movement solver, and prevented entrance separation from forming a doorway blockade. |
| Physical check-in | Replaced the split legacy/modern completion with one canonical finalizer. Physical payment now sets nested payment/check-in/course-access state, compatibility fields, golf events, outcomes, and cart assignment exactly once. |
| QA drivers | Updated title-card, laptop, inventory, course-editor, furniture, register, and golf routes to current production controls and state names; fixture-only isolation uses supported reset/reversal APIs. |
| Office interactions | The generated clear-side laptop access point was 2.35 m from a 2.30 m interaction and the nearer wall map stole focus. The laptop now covers the authored approach and wins while faced, with a focused source contract and a normal-`E` browser check. |
| Tool-wheel input | The wheel advertised mouse input but inherited `pointer-events: none`, allowing the canvas to intercept clicks. The modal root now owns pointer events; keyboard behavior is unchanged. |

Every resolution retained the domain owner and added a focused regression. No conflict was settled by discarding an entire side's production subsystem.
